1. I want each ticket to have {slug}-{number(auto-increment)}.
2. Please add a help icon, which redirects to the official documentation of model names that the claude and codex CLI can use on the governance page's model selection area.
3. I want to be able to delete project. When the users try to delete the projects, I wish to be able to select to terminate the actual project directory. Of course, the mapping data must be deleted as well.

Add the feature explanation to `system-manual.html` about the new features after implementation.

